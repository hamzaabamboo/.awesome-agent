# Specification: Standardize Sync Engine (Archived)

## Context
This track documents an older architecture discussion that has been superseded by the current `skills.sh`-based model.

## Status
Superseded. The repo now uses `skills.sh` for remote skills and stores only local custom skills in-repo.
